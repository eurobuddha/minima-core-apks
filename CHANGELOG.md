# Changelog

- 2026-06-24 · minima-utxo-wallet 0.3.1 — Reduce node load to keep Minima Core stable until its IPC is hardened. The per-reload command set drops from 6 cmds/~28KB to 4 cmds/~1.4KB: 'scripts' (~27KB, the wallet's stable 64-address pool) is now fetched once on load then only every ~20 blocks instead of every reload; 'getaddress' moved off the reload loop to a lazy fetch when the Receive tab is opened (result cached so display + Copy both work — also fixes a latent copy bug); reload debounce raised 200ms->400ms to coalesce the NEWBLOCK+NEWBALANCE burst into one reload. block/coins/balance still fetched every block (spend decisions + Distribute need fresh coins). No behaviour change to History/Wallet/Send
- 2026-06-24 · minima-utxo-wallet 0.3.0 — History rebuilt from scratch, node-friendly (lessons from myHistory). Shows SENT + RECEIVED + SELF, classified client-side from the node's history. The heavy 'history' command is now called ONLY while the History tab is visible and at most once per new block (this every-reload hammering is what crashed Minima Core) — bounded offset pagination (max:25 per call, ~350KB, well under the IPC limit; 'Load older' pages via offset, never a giant call). Lazy detail on tap; in-flight sends merged on top and auto-confirmed with the real on-chain txpowid + working explorer link by matching spent inputs; stale sends reconciled; Re-post/Dismiss on failed. TxResolver removed. Includes all code-review fixes (pagination, stale-pending fallback, expand-state stability, onResume refetch, multi-token hint)
- 2026-06-24 · minima-utxo-wallet 0.2.2 — HOTFIX: stop crashing Minima Core. Root cause (diagnosed via 3 explore agents + live node): 0.2.1's background txpowid resolver called the node's heavy 'history' command on EVERY reload (every block/balance event) forever, hammering the node's single-threaded IPC receiver until it crashed. Removed that every-reload call entirely. Also made History row details build lazily on tap instead of eagerly (no more main-thread view explosion). Full History rebuild (sent+received, node-friendly) ships next in 0.3.0
- 2026-06-24 · minima-utxo-wallet 0.2.1 — History rebuilt to match the dapp 1:1 — expandable Details with FROM (every input address + amount), TO (every output), CHANGE (yours/external tag), BURN, TX HASH (Explorer link), INTERNAL ID, NOTE; every value tap-to-copy; status pills Sending/Posted/Confirmed/Failed; Re-post + Dismiss on failed rows. CRITICAL explorer fix verified on a live node: the txnsign-response txpowid is NOT the on-chain id — the app now resolves the REAL txpowid by matching the spent input coinids against the node's history command, marks the row Confirmed, and only then shows the (working) Explorer link. Inputs/outputs now stored for Send/Split/Consolidate/Distribute
- 2026-06-24 · minima-utxo-wallet 0.2.0 — Design toggle is now Dark (current) <-> Light (the original brutalist look) — no more redundant second dark; every screen (Wallet/Balances/Receive/Send/History) is themed via the token engine. History entries now have an expandable Details section (FROM inputs / TO outputs / CHANGE / BURN / TX HASH with explorer link / INTERNAL ID / NOTE), like the original. Explorer link only opens for a real confirmed txpowid. Fixed scripts filter (default-only) so contract scripts no longer pollute wallet addresses, verified against a live node
- 2026-06-24 · minima-utxo-wallet 0.1.9 — Fix top status-bar overlap (handle window insets); Send posting now shows staged progress (Building → Adding inputs → Adding outputs → Signing & posting) and dims the button so it no longer looks stalled; History explorer link only opens for a real confirmed txpowid (0x+64hex) so it stops opening unreachable pages; add a design-language toggle in the header (Current ↔ Original-Dark; Original-Light + full theming across all screens lands next)
- 2026-06-24 · minima-utxo-wallet 0.1.8 — Send is now full UTXO transaction construction (true to the dapp): shows the FROM input coins + total, recipient, amount + MAX, an editable Change address + NEXT (rotates your node's default addresses), and Minima burn. Preview opens a Confirm breakdown — Inputs / Outputs / Change / Burn + irreversibility warning — before Sign & Post. Recipient validated via node checkaddress; amount clamped to token decimals; change math total-amount-burn (zero-change allowed)
- 2026-06-24 · minima-utxo-wallet 0.1.7 — Fix false 'Minima Core didn't respond' error on consolidate/send — IPC timeout is now per-command (reads 30s; transaction/PoW commands 180s) so slow proof-of-work no longer trips a fake failure while the tx actually posts
- 2026-06-24 · minima-utxo-wallet 0.1.6 — Wallet UX: Tools moved off a separate tab into a Tools dropdown on the Wallet page (Split/Consolidate/Distribute/Untrack); restored per-address COPY, tap-to-copy coinid, and collapsible address cards; Consolidate reworked (no selection = consolidate Minima 0x00; 2+ selected = merge exactly those); distribute progress shown on the wallet page
- 2026-06-24 · minima-utxo-wallet 0.1.5 — Reconciled parsers against the node's command JSON (coins/balance/checkaddress/scripts/block/txn-outputs all confirmed correct); honest Coin.confirmed (no phantom blkconfirmed field)
- 2026-06-24 · minima-utxo-wallet 0.1.4 — Review round 2: node checkaddress validation on send; form-agnostic change-coin match; Distribute reservation closes double-start window; bounded image LruCache + off-thread QR/icon decode; NodeApi cancels pending timeouts on destroy; release.sh fails closed when GitHub is unreachable
- 2026-06-24 · minima-utxo-wallet 0.1.3 — Code-review fixes: validate recipient address (no command injection); Distribute tracks exact change coinid + retries mid-chain failures (no stranded jobs); clamp amounts to token decimals; non-destructive history DB upgrades; debounced reloads; guard callbacks against destroyed activity
- 2026-06-23 · minima-faucet 0.1.1 — Native Faucet companion app (getaddress over IPC + faucet backend request)
- 2026-06-23 · minima-utxo-wallet 0.1.2 — Wallet tab: list addresses with coins first, empty addresses below (match the MiniDapp UX)
- 2026-06-23 · minima-utxo-wallet 0.1.1 — Multi-batch Distribute (spread across up to 56 addresses); native UTXO wallet (Wallet/Balances/Receive/Send/Tools/History) over the IPC
